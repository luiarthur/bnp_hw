#  mu = (mu_z, mu_x)
#   S = S_{zz}, S_{zx}
#       S_{xz}, S_{xx}

# [y_i , x_i1, x_ik] ~ Bern(y_i, p_i) x N_k(mu_x, S_{xx}), 
#   for S_{zx} = 0, 
#   p_i = mu_z / sqrt(S_zz), where S_zz is a constant 
# mu_x 
